spring:
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
    generate-ddl: false
  datasource:
    driver-class-name: org.postgresql.Driver
    url: "jdbc:postgresql://pg_devops:5432/db"
    username: usr
    password: pwd