- name : Open port 80
  firewalld:
    port: 80/tcp
    permanent: true
    state: enabled
    immediate: true
- name : Open port 22
  firewalld:
    port: 22/tcp
    permanent: true
    state: enabled
    immediate: true
